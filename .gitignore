# ─── TwinCurrent (must never commit) ────────────────────────────────
.twincurrent-build/

# ─── Fredrik's personal scratch (notes, ledgers, never shared) ──────
docs/fredrik/

# ─── Claude Code (local agent state, permission grants, MCP creds) ──
.claude/

# ─── Environment & secrets ──────────────────────────────────────────
.env
.env.*
!.env.example

# ─── Foundry (contracts/ only — scoped so Next.js paths like web/lib/ aren't matched) ───
contracts/out/
contracts/cache/
contracts/broadcast/
contracts/.foundry/
# Note: contracts/lib/ is NOT ignored — Foundry tracks dependencies as git submodules there.
# Submodule contents live in their own repos; only the .gitmodules pointer is committed.
contracts/dependencies/

# ─── Node / Next.js (web/) ──────────────────────────────────────────
node_modules/
.next/
dist/
build/
.vercel/
.turbo/
*.tsbuildinfo
next-env.d.ts

# ─── Logs ───────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ─── Editors / OS ───────────────────────────────────────────────────
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini
.playwright-mcp/
