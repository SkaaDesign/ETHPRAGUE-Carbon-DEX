{
  "actorSpecification": 1,
  "fields": {
    "type": "object",
    "properties": {
      "price": { "type": "number", "description": "Current EUA spot price (EUR per allowance)" },
      "currency": { "type": "string", "description": "ISO currency code, always 'EUR' for EUA" },
      "source": { "type": "string", "description": "Human-readable source label (e.g. 'Trading Economics')" },
      "url": { "type": "string", "description": "Public URL where the figure was sourced" },
      "fetchedAt": { "type": "string", "description": "ISO-8601 timestamp of when the actor ran" }
    },
    "required": ["price", "currency", "source", "fetchedAt"]
  },
  "views": {
    "overview": {
      "title": "Latest EUA spot",
      "transformation": { "fields": ["price", "currency", "source", "fetchedAt"] },
      "display": {
        "component": "table",
        "properties": {
          "price": { "label": "Price (EUR)" },
          "currency": { "label": "Currency" },
          "source": { "label": "Source" },
          "fetchedAt": { "label": "Fetched at" }
        }
      }
    }
  }
}
